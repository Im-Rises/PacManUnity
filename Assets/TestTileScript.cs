using UnityEngine;
using UnityEngine.Tilemaps;

public class TestTileScript : MonoBehaviour
{
    public Tilemap tilemap;

    private void Update()
    {
        // var tile = tilemap.WorldToCell(transform.position + new Vector3(Vector2.left.x, Vector2.left.y, 0));
        // Debug.Log(tilemap.HasTile(tile));
    }
}
